name: My GitHub Action

on: [push]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
    - name: My Step
      run: echo "My Step has completed."
    - name: Set conclusion to neutral
      run: echo "::set-output name=conclusion::neutral"
